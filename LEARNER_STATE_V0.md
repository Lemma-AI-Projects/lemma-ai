# Learner State V0

让 Lemma 第一次能回答：**这个用户在这个 Learn Space 里，哪些已经掌握、哪些还没有、下一步可以学什么。**

答案不是模型猜的，是从**证据**里按 ALEKS / KST 的核心思路**算**出来的：一个确定性的纯函数，
零概率、零参数、零模型调用（`backend/ai/knowledge/state.py`）。

**这一版刻意不做**：Knowledge Tracing / Neural KT · Cognitive Diagnosis · 用户画像 ·
自动课程规划 · Coordinator · Scheduler · Method Set · Multi-Agent · 复杂 assessment engine ·
semantic learner model。计划：`planning/learner-state-v1-execution-plan.md`。

---

## 1. 实现了什么

```text
用户作答
   ↓
Evidence（唯一写入面：Agent 的 record_evidence 工具）
   ↓
Knowledge State（纯函数派生：下集 + 两条闭包规则）
   ↓
Learner State（已具备 / 正在形成 / 接下来可学 / 没测过）
   ↓
Global Agent 读它 → 回答「下一步学什么」
```

| 环节 | 做法 |
|---|---|
| **结构** | `knowledge_items` + `knowledge_edges`（先修偏序，必须无环） |
| **证据** | `knowledge_evidence`：只有对错 + 可靠性等级 + 依据。**没有分数** |
| **状态** | **不落库**，每次从证据现算。删掉任意一条证据，状态必须能重算 |
| **读** | 每轮装配把「学习状态」拼进提示词，并记进这条回答的 digest |
| **写** | Agent 只有一个 `record_evidence` 工具；**没有任何「设置状态」的接口** |
| **看** | 工作台左侧「学习简报」里的 **知识结构** 一块：✓ / → / ○ |

**三条红线**（承接 EFF 6/7/8）：① Agent **写证据、不写状态**（靠权限：工具表里没有改状态的工具）
② 状态**派生**、不缓存 ③ **不外流任何数值**（没有百分比、没有掌握度、没有权重）。

---

## 2. 打开哪个页面

工作台 → 进 **「AI for Math」** → 左侧 **学习简报** 面板。

面板里从上到下是：学习目标 · **知识结构** · 你正在做 / 你已经具备 / 正在形成 / 接下来 · 更新时间。

**知识结构** 那一段就是 Learner State Inspector：

```text
✓ 矩阵基础        ← 已具备
✓ 线性无关
→ 特征值          ← 前提都具备了，接下来可学
○ 特征向量        ← 还没轮到（它的前提还没过）
○ 对角化
```

- ✓ = 已具备（`mastered`）· → = 接下来可学（外边缘）· ○ = 还没轮到
- 后面的「N 条记录」是这条结论背后的证据条数 —— 是计数，不是得分
- 顺序是**先修顺序**（拓扑排序），所以「下一步」永远紧跟在已具备那一段的后面

---

## 3. 怎么完成 Demo

演示空间里的知识结构是一条链（脚本灌进去的，5 项 4 边）：

```text
矩阵基础 → 线性无关 → 特征值 → 特征向量 → 对角化
```

**第 1 步 · 做一道题**（让状态动起来）。在空间里对 Agent 说：

```
我刚做了一道矩阵乘法的题：A = [[1,2],[3,4]]，B = [[5,6],[7,8]]，
我算的 AB = [[19,22],[43,50]]。请核对一下我算得对不对，并按我们的约定记录。
```

Agent 会核对算式、调用 `record_evidence`（这类有确定对照的记为 `verified`，
一条就能定案），然后告诉你结果。**此时面板变成 `✓ 矩阵基础 / → 线性无关`。**

> 如果它只是点评没记录，直接说一句「麻烦把我刚才那道题的作答按约定记录下来」即可 ——
> 写不写是它的判断，这是 V0 的已知简化（见 §6）。

**第 2 步 · 问下一步**：

```
我下一步适合学什么？
```

它应该说 **线性无关**（而不是泛泛推荐）。

**第 3 步 · 再答一道（线性无关）**，然后再问一次 → 应该得到 **特征值**。

**第 4 步 · 故意答错一道特征值的题**，例如：

```
我做了一道求特征值的题：A = [[2,0],[0,3]]。我写的是 λ = 2 和 λ = 4（我把右下角看成了 4）。
```

面板应该**不把它标成 ✓**，而是仍然 `→ 特征值`，并且 `特征向量 / 对角化` 保持 ○
（上面的东西被没过的前提挡住了）。

**第 5 步 · 一条反例**：说一句「我会做特征向量」，它**不会**变 ✓ ——
自述不是证据，它应该出题去验。

---

## 4. 状态怎么从 A → B → C 前进

链上是 `矩阵基础(A) → 线性无关(B) → 特征值(C) → …`：

| 时点 | 证据 | 派生结果 |
|---|---|---|
| 一开始 | 无 | 全部 `没测过`；**外边缘 = {矩阵基础}**（唯一没有前提的项） |
| 答对 A | A: correct / tier A | A `已具备`；**外边缘 = {线性无关}** |
| 答对 B | B: correct / tier A | A、B `已具备`；**外边缘 = {特征值}** |
| 答错 C | C: incorrect / tier A | C `没做对`；**外边缘 = {特征值}**（没做对的项仍是可做的下一步，但它的下游全被挡住） |

两条闭包规则是这套东西的全部机制：

- **向下**：做得了 B ⇒ 一定做得了 A（所以「答对 B」会连带把 A 判为已具备 —— 见上面的实测）
- **向上**：做不了 A ⇒ 一定做不了 B（所以答错 C 之后，D、E 不会被说成已具备）

---

## 5. Global Agent 怎么用它

- 每轮把**派生出来的**状态拼进提示词（`## 学习状态（由系统根据证据算出，请以此为准）`），
  并附一句纪律：不要自行推断掌握程度、不要杜撰清单以外的知识点、要产生新证据就出题。
- 提示词里的这份状态**在回合开始就固定**（与 prompt 同一时刻）；回答的 digest 里会记下
  `learnerState.ready`（本轮被告知「接下来可学」的是哪几项），面板因此可以说清
  「它当时看到的是这个」。
- Agent **自己不改状态**：它只能写证据，状态由系统重算。工具返回里带着写入**之后**的
  itemState，要求它如实转述，并且用词要对上（mastered / not_mastered / unassessed）。

---

## 6. 哪些地方只是 V0 简化（如实列出）

1. **演示结构只有 5 项一条链**（脚本灌的），不是真实知识库。真正的 Phase 0 要由模型起草
   + 人审（计划 §A）。
2. **写入靠 Agent 判断**：它没调 `record_evidence`，这一轮就没有证据 —— 用户需要说一句
   「记一下」（测试脚本也这么做）。
3. **可靠性只有两级**：`verified`（有确定对照，一条定案）与 `judged`（按要点判断，要两条）。
   没有更细的 slip/guess 参数 —— 没有标定数据，算出来的概率是编出来的数字。
4. **不做遗忘曲线、不做重测调度**：证据只增不减，一条旧的对错永远算数。
5. **冲突取保守**：同一项既有对又有错 ⇒ 判为「没做对」，且结果与证据顺序无关。
6. **自述不接受**（`self_reported` 只留在枚举里）：说「我会」不会改变状态，只能出题验。
7. **结构本身不可在界面上编辑**：只读展示。改结构目前只能走脚本 / 接口。
8. **外边缘不排序、不排优先级**：它是一组「现在可学」，不是一份推荐顺序。

与 Space Memory 的边界保持不变：**记忆 = 这个空间过去发生了什么；学习状态 = 根据证据目前认为
用户掌握了什么。** 状态不进记忆，记忆也不承载掌握度。

---

## 7. 怎么跑起来

```bash
cd .workbuddy/localdb && ./pg.sh start          # 本机 PostgreSQL 17.6 @ 127.0.0.1:55432

cd backend
.venv/Scripts/python.exe -m alembic upgrade head        # 知识三表（若还没建）

# 灌演示结构（可重复执行；--reset 先清空，让演示从「什么都没测过」开始）
.venv/Scripts/python.exe scripts/seed_demo_knowledge.py --space "AI for Math" --reset

# 仓库测试（无数据库时 DB 那两组会自动 skip）
"D:/github projects/lemma-ai/backend/.venv/Scripts/python.exe" -m pytest tests -q

# 端到端（真模型，走 Test A–D 并核对派生结果）
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe \
  ../../../.workbuddy/localdb/verify_learner_state.py
```

前端与后端：后端 `127.0.0.1:8001`、前端 `http://localhost:5173`。
